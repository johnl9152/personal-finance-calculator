# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Personal portfolio site for John Lee. The repository is in early setup — no framework or tooling has been initialized yet.

## Getting Started

Once the project is initialized, update this file with:
- Build, dev, and lint commands from `package.json` scripts
- How to run tests (single test and full suite)
- Any environment variables required (without values)

## Architecture

To be documented once the project structure is established. Update this section with:
- Framework and routing approach (e.g., Next.js App Router vs Pages Router)
- How content/projects data is managed (static JSON, MDX, CMS, etc.)
- Component organization and naming conventions
- Styling approach (Tailwind, CSS Modules, etc.)
